# Default frame selections. Be sure this matches public/placeholder_frame.svg. If you change these values, replace that file (copy the matching device's synced SVG from public/frames/).
NEXT_PUBLIC_DEFAULT_CATEGORY=phones
NEXT_PUBLIC_DEFAULT_DEVICE=iphone-16-pro-max
NEXT_PUBLIC_DEFAULT_VARIATION=desert-titanium
